# 2-max_int

This project implements a method that returns the maximum integer in a list.

## MyMath.Operations.Max

Returns the maximum integer from a list of integers.

### Usage
```csharp
var nums = new List<int> { 3, 7, 2, 9, 1 };
int max = MyMath.Operations.Max(nums); // Returns 9
```